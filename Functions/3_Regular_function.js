// Regular function là một hàm đơn giản và có thể là đầu tiên bạn phải học khi bước vào ngôn ngữ lập trình. Ở đây chúng ta có thể kha báo function keyword, function name, parameters, và body như ví dụ bên dưới

function add(one, two) {
    return one + two;
}

console.log( add(1,2) );